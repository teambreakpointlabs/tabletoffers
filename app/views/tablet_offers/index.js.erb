/* error if put under javascripts directory */
$('#offers').append("<%= escape_javascript(render :partial => @tablet_offers)%>");
$('#load_more_link').replaceWith("<%= escape_javascript(link_to_next_page(@tablet_offers, 'Load More', :remote => true, :id=>'load_more_link', :params => params))%>");
